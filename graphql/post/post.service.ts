/* import { useQuery } from "@apollo/client"
import { PostFields } from "../models/post.model"
import { USER_POST } from "./post.query"

export class postService {
  queryUserPosts(fields: PostFields){
    const {data, loading, error} = useQuery(USER_POST(fields), {})


  }
} */

export {};
